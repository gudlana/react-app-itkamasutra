import React from "react";
import preloader from "../../../assets/image/three-dots.svg"

let Preloader = (props) => {
    return <div>
         <img src={preloader} />
    </div>
}

export default Preloader;